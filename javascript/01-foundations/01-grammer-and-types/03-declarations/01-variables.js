/*
three kinds of variable declarations:
1. var: local or global, can be re-declared
2. let: block-scoped (local), can be updated, can't be re-declared in the same scope
3. const: block-scoped (local), read-only
*/

/*
rules for identifiers (name of variables)
- start with: a letter, underscore (_), or dollar sign ($)
- subsequent characters: digits (0 – 9),  A through Z (uppercase), a through z (lowercase)
- unicode letters: å, ü
*/

var Number_hits;
var temp99;
var $credit;
var $name;